#include <iostream>
#include <vector>
using namespace std;
int main() {

    int n, query;
    cin >> n >> query;
    vector<int> numbers;

    for (int i = 0; i < n; i++)
    {
        int k;
        cin >> k;
        numbers.push_back(k);
    }
    

    return 0;
}
